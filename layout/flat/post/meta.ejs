<p class="post-meta">
    <time class="post-category post-category-design" datetime="<%= date_xml(post.date) %>" itemprop="datePublished">
        <%= date(post.date, null) %>
    </time>
    <% if (post.tags && post.tags.length){ %>
    <% count = post.tags.length %>
    <% post.tags.forEach(function(tag, index) { %>
    <a class="post-category post-category-pure" href='<%= config.root + tag.path %>'><%= tag.name %></a>
    <% if (count !== index + 1) { %>
    ,
    <% } %>
    <% }); %>
    <% } %>
</p>